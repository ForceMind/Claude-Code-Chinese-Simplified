# 开发笔记：从零到现在的经过、思路与后续计划

> 这份文档记录 **Claude-Code-Chinese-Simplified** 从零开始的完整心路历程、关键决策的推理过程，以及后续的方案计划。
> 目的：让开发可以随时中断、随时接续，不丢上下文。
>
> 最后更新：2026-08-03

---

## 一、我们在做什么

打造一个**通用、开源、可持续的 Claude Code 汉化工具**，造福大家。

**软件的灵魂（最高优先级，一切决策的准绳）：**

> ### 可持续 + 简单

任何设计，若违背这两个字，就推翻重来。

---

## 二、缘起：为什么要自己做

### 痛点

1. **Claude Code 变成了原生二进制。** 早期 CC 是 Node 的 `cli.js`，改 JS 文本即可汉化。现在 CC 是 **Bun 编译的原生单文件可执行程序**（`claude.exe`，约 253MB，Windows 下是 PE 格式，Mac 下是 Mach-O），JS 包被嵌入在 exe 里。汉化 = **二进制补丁**，门槛陡增。

2. **现有方案 claude-code-zh-cn 跟不上了。** 参考项目 `claude-code-zh-cn`（作者 taekchef，v0.1.84）质量很高，但：
   - 更新缓慢，作者可能已弃坑；
   - 有 **support-window（支持窗口）** 机制，锁死在 `maxVersion 2.1.195`；
   - 一旦 CC 版本超出窗口，它就**拒绝工作**，界面退回英文。
   - 用户实测：**早已超出它的支持窗口**，且 CC Switch 更新后终端和切换器都退回英文。

3. **翻译滞后是本质矛盾。** CC 更新太频繁，人工维护的词典永远追不上新版本新增的英文文案。

### 我们的立场

> **本工具必须永久支持最新 CC，突破版本窗口限制。**

宁可翻译覆盖不到 100%，也要保证**任何新版本都能立刻用、不被锁死**。
（当前观测到的 CC 版本：**2.1.190**。）

---

## 三、探索过程：我们是怎么想清楚的

### 第 1 步：搞清楚 claude.exe 的内部结构

CC 是 Bun 编译产物。Bun 二进制的关键结构：

- 尾部魔术串 `BUN_TRAILER = "\n---- Bun! ----\n"`（用来识别是不是 Bun 二进制）；
- 偏移表（offsets）、字符串指针表（每个 8 字节，含长度）、编译模块、以及**嵌入的 JS 源码**。

### 第 2 步：读懂 claude-code-zh-cn 是怎么做的

逆向阅读了它的实现（在 `%USERPROFILE%\.claude\plugins\claude-code-zh-cn\`）：

- `bun-binary-io.js`（LIEF 二进制 I/O 核心）——用 **LIEF 库**正式解析 PE/Mach-O，抽取嵌入的 JS 源，改完再**重打包**（`repackPE` / `repackMachO`），Mach-O 还要重新签名。
- `patch-cli.js`——补丁引擎：抽取 JS 源文本 → `applyTranslations(sourceText, map)` 做文本替换 → 重打包写回。
- `cli-translations.json`——文本词典，`[{en, zh}]` 格式，1890 条（我们把它作为上游词典基础）。

**它的方案本质：解析二进制结构 → 抽出 JS 文本 → 自由长度替换 → 重打包。** 强大，但**依赖 LIEF 原生库、流程复杂**。

### 第 3 步：关键的架构分岔

摆在面前两条路：

| 方案 | 优点 | 缺点 |
|---|---|---|
| **A. 等长原地字节替换**（零依赖） | 极简、无第三方依赖、任意版本可跑、真正可持续 | 中文字节数必须 ≤ 英文，超长的装不下 |
| **B. LIEF 解析+重打包**（照搬 zh-cn） | 翻译不受长度限制，可 100% 覆盖 | 依赖 LIEF 原生库、安装复杂、与"简单"冲突 |

### 决策：选 A（等长原地替换，零依赖）

**推理：**

1. **契合灵魂。** B 的重打包依赖 LIEF 原生库，安装复杂，直接违背"简单"。A 是一个无依赖的单文件脚本，才是真正的"可持续"（不会因依赖腐烂而失效）。

2. **A 的可行性论据（2026-07-10 实证修正）。** 早先以为"现役 exe 里有 800 处等长替换"，复核发现**不成立**——现役 claude.exe 比备份小 5246 字节，zh-cn 走的是重打包，长度会变。**真正成立的论据是**：词典里的中文 UTF-8 串确实存在于**正在运行的** claude.exe 内嵌 JS 中（多条实测 `zhInCur=true`），Bun 照常执行 → **Bun 运行时不校验内嵌 JS 完整性**。等长原地替换是重打包的严格子集（不动任何偏移表，更保守），安全性只强不弱。另有语法级验证兜底（见 §九）。

3. **A 天然永久支持新版。** 词典是 `en→zh` 的**文本对，与版本号无关**。只要新版 claude.exe 里还含那句英文，补丁就能命中。**新版新增的英文串**不在词典里 → 不硬报错、直接跳过 → 由 `scan` 报告出来供增量补充。这正好实现"永久支持 + 突破版本窗口"。

**关于覆盖率的取舍（诚实记录）：**

等长方案的代价是覆盖率。上游 1890 条词典里：
- **1177 条**中文字节 ≤ 英文，可直接用（等长安全）；
- **477 条**中文比英文长，等长装不下（暂存 `dict/oversize.json`，留给未来处理）；
- 217 条无效（无中文/非 ASCII 等）。

**等长覆盖率 ≈ 70.4%。** 我们接受这个 trade-off：

> **70% 中文界面 + 永久可用**，胜过 **100% 翻译但被锁死在旧版本**。

剩下 30% 由后续的增量机制和（可选的）重打包引擎逐步补齐。

---

## 四、核心机制设计

### 4.1 等长原地替换算法

对词典里每条 `en → zh`（已保证 `byteLen(zh) ≤ byteLen(en)`）：

1. 在二进制里搜索 `en` 的 ASCII 字节序列；
2. 命中处替换为 `zh 的 UTF-8 字节 + 0x20 空格填充`，凑满 `en` 的字节长度；
3. **字节长度不变** → 二进制结构不破坏 → 无需重打包。

词典按 `byteLen(en)` **降序**排列：先替长串，避免长串被短串的子串抢先替换而错位。

### 4.2 幂等 + 跟随新版（patcher 的关键设计）

**始终从"纯英文备份"出发打补丁，绝不在已汉化的产物上增量叠加。** 流程：

- 读 target，检测它当前是否**纯英文**（不含中文）：
  - **是纯英文**（新装/升级后的官方原版）→ 刷新备份 `copyFileSync(target, target+'.cchans-backup')`，以它为源。**这一步让工具自然跟随新版本。**
  - **已汉化**（我们或别人上次的产物）→ 用现有备份作纯英文源；找不到备份则拒绝并提示重装 CC。
- 从纯英文源做等长替换，写回 target。
- 断言：输出字节数 == 输入字节数（等长不变量）。

**硬链接注意：** CC 的 `install.cjs` 用 `linkSync`（硬链接）放置 claude.exe，所以备份必须用 `copyFileSync` 断开硬链接，避免备份和原文件是同一 inode。

### 4.3 scan：滞后检测 + 贡献/更新提醒（用户明确要求）

当 `scan` 发现"词典有、但此版 CC 里没有命中"或"CC 里有新的未译英文串"时，**按运行者身份给不同提示**：

- **维护者**（工具根目录存在 `.git`，即克隆的仓库）→
  提示："发现 N 处未覆盖，可在**本仓库启动 Claude Code**，让它翻译新串、更新 `dict/sources/cli-translations.json`，再跑 `build-dict`。"
- **普通用户**（无 `.git`，通过 npm 安装）→
  提示："你的 CC 版本可能比词典新。请**更新本工具**（`git pull` / `npm update`）获取最新词典；仍缺失欢迎去仓库提 issue/PR。"

用 `.git` 是否存在来区分身份，简单可靠。

---

## 五、当前进度（2026-08-03 更新：源码路线落地并推送，验证以 §十三 为准）

```
Claude-Code-Chinese-Simplified/
├── LICENSE
├── README.md                       # ✅ 含启动代价/平台状态/安全机制的诚实披露
├── DEVELOPMENT-NOTES.md            # 本文档
├── package.json                    # ✅ 零依赖, bin: cchans(npm 未发布, clone 使用)
├── bin/
│   └── cchans.js                   # ✅ CLI: patch/restore/status/scan/locate
├── tools/
│   └── build-dict.cjs              # ✅ 构建等长安全词典(8 层安全过滤, 含危险词条黑名单)
├── dict/
│   ├── sources/
│   │   └── cli-translations.json   # ✅ 上游词典(claude-code-zh-cn 起步 + 自行增补), 1903 条
│   ├── zh-Hans.json                # ✅ 等长安全词典, 1163 条 (96731 字节)
│   └── oversize.json               # ✅ 超长条目, 477 条 (备用)
├── src/
│   ├── bunfmt.js                   # ✅ Bun 模块表最外层解析(trailer 反推, 不解析 PE/Mach-O/ELF)
│   ├── locate.js                   # ✅ 定位/自愈(显式路径权威, 只读命令不写盘)
│   ├── patcher.js                  # ✅ 补丁引擎(源码区限定/关字节码/编码修正/热替换/锁/实跑验证)
│   └── scan.js                     # ✅ 覆盖扫描(限定源码区) + 滞后诊断
└── test/
    └── selftest.js                 # ✅ 零依赖自测 26 条(node test/selftest.js)
```

**2026-08-03 端到端验证（对 v2.1.220 / 253MB，详见 §十一~§十三）：**
- patch：944/1163 词条命中、2425 处替换，全部限定在 20.7MB 源码区，实测 973 个中文正确渲染、零乱码；
- 幂等三连（patch×2 同 sha / restore=原版逐字节 / 再 patch 同 sha）、区域封闭（区外仅 4+1 字节字段改写）；
- 运行时验证四重判定（启动/崩溃/乱码/生效）双向实测；推送前经三路独立审查放行。

**端到端验证结果（2026-07-10，对当时的 226MB 二进制；以下为该日快照，与上方文件树的当前数字不同步）：**
- patch：980/1151 词条命中，4175 处替换，词边界守卫跳过 438 处代码内命中，约 44s；
- 长度不变、备份与原件逐字节相同、槽位字节 = 中文+空格填充、二次 patch 幂等；
- restore 后与纯英文原件逐字节相同；
- **语法级黄金验证**：从补丁产物抽出内嵌 cli.js（16.5MB），esbuild 0.28 完整解析通过（与原版一致）。

---

## 六、后续方案计划

### 近期（把工具跑通）—— ✅ 已全部完成（2026-07-10，见 §五/§九）

1. ✅ `src/patcher.js`（等长替换 + 幂等/跟随新版 + 备份/还原 + 词边界守卫 + 原子写）。
2. ✅ `src/scan.js`（命中统计 + 滞后判断 + `.git` 身份提醒 + 短词误伤告警）。
3. ✅ `bin/cchans.js` 五个子命令；`package.json` 零依赖。
4. ✅ 端到端验证（字节断言 + esbuild 语法级验证）。**注意：当时未实跑 exe，这个盲区在 2026-08-03 酿成事故，见 §十一；现已内置运行时冒烟验证。**
5. ✅ README。下一步：git commit + 发布。

### 近期 follow-up（2026-08-03 三路审查留下的非阻断项）

6. `noeffect` 判定的安全边际监控：三条冒烟命令合计只有 22 个中文字符，未来 CC 改文案
   可能假失败（失败方向保守）。建议成功日志里打出 cjkSeen 便于观察边际收窄。
7. `scan` 在 bunfmt 解析失败退回全文件时加口径提示（当前静默，命中率会虚高）。
8. macOS / Linux 真机验证（README 已诚实标注"未实测"）。

### 中期（提升覆盖率与自动化）

9. **增量词典维护闭环**：`scan` 导出未译串报告 → 在仓库启动 CC 自动翻译 → 更新上游词典 → `build-dict` 重建。
10. **（可选）SessionStart 钩子**：CC 更新/被 CC Switch 覆盖后，下次启动自动重打补丁。

### 远期（突破等长限制，冲击 100%）—— 大部分已由 §十四 的 `--full` 达成

11. ~~可选的重打包后端~~：**已被 `--full`（§十四）取代大半**——477 条超长条目里
    可在源码区内找到全局预算的 387 条已全部放入，不需要碰模块表。真正还需要
    "重打包"（挪动源码区之外的字节、重算模块表偏移）的，只剩**词典命中不到、
    或全局预算仍不够**的极少数残余场景；`.bun` 是文件物理上最后一个 section
    这条结构性便利（§十三调研）仍然成立，需要时再评估，优先级已大幅降低。

---

## 七、技术参考与注意事项

- **参考实现**：`%USERPROFILE%\.claude\plugins\claude-code-zh-cn\`（taekchef，v0.1.84）。核心 `bun-binary-io.js` / `patch-cli.js` / `cli-translations.json`。
- **CC 二进制位置**（本机）：`C:\nvm4w\nodejs\node_modules\@anthropic-ai\claude-code\bin\claude.exe`（226339362 字节，已汉化）与 `claude.exe.zh-cn-backup`（226344608 字节，纯英文，差 -5246 字节说明 zh-cn 对超长条目做了重打包）。
- **CC 与 conda 无关**：claude 是按绝对路径安装的全局二进制，定位它即可，无需管 conda `nikki` 环境。
- **⚠️ 必须实跑补丁产物去验证**（2026-08-03 修正，此前这里写的是"不要实跑，字节断言即可"——那条建议直接导致了 §十一 的严重事故）。字节断言、词边界守卫、对 cli.js 的语法检查**都发现不了**补丁破坏 Bun 运行时数据区的情况。`patch` 现已内置运行时冒烟验证。注意 `--version` 太浅（补丁坏了也能过），必须叠加 `doctor` 这类走完整启动路径的命令。
- **⚠️ 文件持久化坑**：node/PowerShell 产生文件的命令需带 `dangerouslyDisableSandbox`，并用 `wc -c` 复核字节数确认落盘（`build-dict.cjs` 曾出现报告成功但未持久化，`zh-Hans.json` 当前应为 96731 字节）。
- **Bun 识别**：文件尾部含 `\n---- Bun! ----\n` 且体积 >20MB 即判定为 claude 原生二进制。

---

## 八、致谢

本工具站在 [claude-code-zh-cn](https://github.com/taekchef/claude-code-zh-cn)（作者 taekchef）的肩膀上——其词典与二进制补丁思路是本项目的重要起点。我们的差异化在于：**用零依赖的等长原地替换换取极致的简单与可持续，以永久支持最新版 Claude Code 为第一目标。**

---

## 九、2026-07-10 开发冲刺实录：顾问审阅 + 三个实证反转

按「先计划 → 问顾问 → 确认无问题 → 再开发」的流程走完了全程。顾问审阅 + 自主实证共发现三个**教科书式的反转**，全部用最低成本实验裁决，记录如下：

### 反转 1：等长方案的立论换了根基（见 §三 修正）

旧论据"现役 exe 有 800 处等长替换"被字节对比推翻（现役比备份小 5246 字节 = zh-cn 是重打包）。新论据更强：中文串实测活在运行中的内嵌 JS 里 → Bun 不校验 JS 完整性 → 等长是重打包的严格子集。

### 反转 2：`\uXXXX` 转义该保留，顾问建议反了

顾问认为词典里的字面 `—` 等转义是"抽取态污染"，应反解成真实 UTF-8 再匹配。**实测正相反**：二进制内嵌的是 JS 源码文本，源码里写的就是 `—` 这 6 个 ASCII 字符——8 条样本 7 条以字面形态直接命中，反解后全部打不中。**结论：保持原样。**（顾问的另一半意见是对的：`${...}` 占位符条目 30 条确实是死条目，且含混淆后的版本特定标识符，已剔除。）

### 反转 3：短词命中在标识符内部——顾问和我们都漏掉的 A 级风险

scan 的短词告警引出关键发现：`Sandbox` 434 处命中里 **301 处在压缩代码的标识符内部**（`SandboxNetworkAccess`、`queryCancelledError`、`supportsAdaptiveThinking:`）。等长替换的**填充空格会劈开标识符 → 语法错误 → 二进制报废**。zh-cn 的无填充替换侥幸免疫（CJK 是合法标识符字符，全局替换 = 一致改名）。修复双管齐下：

1. **patcher 词边界守卫**：needle 边缘字节是标识符字符时，命中位置相邻字节也是标识符字符则跳过（实测跳过 438 处）。
2. **build-dict 剔除裸标识符词**（`/^[A-Za-z][A-Za-z0-9_$]*$/`，11 条）：独立属性名 `{Sandbox:1}` 与被填充的字符串键 `"沙盒 "` 会不一致，整词剔除才彻底。

### 语法级黄金验证（新增的验证手段）

手工解析 PE section 表（40 字节/项，零依赖）→ 定位 `.bun` section → 按 `[size header][blob…trailer]` 结构抽出内嵌模块 → 主模块 `cli.js` 16.5MB。**补丁前后都用 esbuild 完整解析：均通过。** 注意 `node --check` 不可用（Node 22 不认 ES2026 的 `using` 语法，原版也过不了，属基线限制）。抽取脚本在开发 scratchpad，未进仓库（工具运行时不需要它）。

### 顾问其余结论的落实情况

- B1 性能（43s）：加进度提示，接受（每次升级跑一次，可忍）。
- B2 备份误判：已用通用 CJK 连续串检测（连续 ≥4 个 CJK 字符的串 ≥50 处判已汉化；实测纯英文原版仅 7 处、汉化版 200+，区分度充足），覆盖旧备份前先比对 + 保留 `.prev`。
- B3 Windows 占用：atomicWrite 捕获 EBUSY/EPERM 转友好提示。
- C 级伪风险（PE 签名/checksum/Bun 自校验/空格破坏字符串语义）：确认不拦截，不处理。

### 教训

1. **「继承来的证据」也要复核**——"800 处 sameSlot"是上一轮分析的结论，差点带着错误论据发布。
2. **顾问的价值在提出问题，裁决靠实验**——顾问指出词典污染方向完全正确，但具体修法（反解转义）反了；30 分钟实验避免了把 15% 死条目"修"成 100% 死条目。
3. **告警机制会反哺设计**——scan 的短词告警本来只是 B4 的"可观测性"需求，却直接钓出了最严重的 A 级 bug。

---

## 十、2026-08-03 实录：撞见 CC 自更新 daemon 与 claude-code-zh-cn 打架，顺带扩充词典

### 现象：claude.exe 在会话过程中"自己变了"

在本机（nvm v22.22.1 环境）执行 `npm update -g @anthropic-ai/claude-code` 时报 `EBUSY`（被运行中的进程占用），npm 自己的 debug log 证实这次更新在"挪走旧文件"那一步就整体中止，**没有写入任何新内容**。但几乎同一时刻，`cchans status` 显示目标文件从 253.4MB/纯英文 变成了 215.9MB/已汉化（CJK 连续片段 2299 处，远超"已汉化"判定阈值 50，也远超纯英文基线的 7 处），且没有 `.cchans-backup`。

### 根因：不是 cchans，是另一条自动化链路

排查 `~/.claude/daemon.log` 发现真相：**Claude Code 自带一个后台 supervisor daemon**，会持续监视自身二进制文件的 mtime，一旦变化就自动重启进程完成"自升级"：

```
[supervisor] binary at ...claude.exe changed (mtime changed) — self-restarting for upgrade
[supervisor] ─── daemon start ─── version=2.1.190 pid=...
```

同时 `~/.claude/settings.json.zh-cn-backup.*`（Jun 26 / Jul 1 的文件）证实本机曾经配置过 **claude-code-zh-cn**（命名规则是它的备份约定，不是 cchans 的）。claude-code-zh-cn 有版本白名单（见 §二 痛点 2），遇到 CC 自升级到白名单之外的版本时，很可能触发了它自己的自动化（计划任务/hook），把二进制拉回它认识的旧版本（2.1.190）重新打了汉化补丁——这正是 cchans 存在的理由本身在本机演了一遍。

### 处理方式

1. `npm install -g @anthropic-ai/claude-code@latest --force` 强制干净重装，拿到官方纯英文 2.1.220（253.4MB）。
2. `cchans patch` 打上 cchans 自己的补丁并验证（体积不变、状态正确、备份就位）。
3. **教训**：如果机器上同时装了 claude-code-zh-cn 一类会自动重新触发的旧工具，它可能在后台把 cchans 刚打的补丁悄悄替换掉。cchans 目前不检测"是否被别的工具覆盖"，只保证自己每次执行时的正确性。**待办**：`status`/`patch` 或许应该识别出"已汉化但不是本工具产物"的情形（比如 CJK 密度远超本工具词典能产生的量级）并给出更明确的提示，而不是把它当成普通的"已汉化，直接用备份"。

### 词典增补：194/207 未命中里，只有一小部分能被机械找回

对着 v2.1.220 跑 `scan --prompt` 拿到 207 条未命中。**第一次尝试**是把这 207 条原样重新收录并翻译——结果全部 0 效——因为它们**本来就已经在词典里**（旧版本遗留），未命中的真正原因是**对应英文文案在新版里已被改写或删除**，不是"词典缺条目"。

正确做法：在纯英文备份里为每条未命中项做**递减长度锚点搜索**（80/60/45/30/22/16/12/9 字节前缀和后缀），定位现存的相邻文本：
- 锚点 ≥30 字节的匹配基本可信（27 条）；
- <30 字节的匹配（163 条）绝大多数是撞上了 Bun 打包进二进制的第三方库文本噪音（SQLite、ripgrep、ICU、libjpeg-turbo 等），不能当真；
- 完全无锚点命中（17 条）大概率是功能被彻底移除。

对 27 条强信号逐一核实完整文案（含辨别"remote→cloud"这类系统性改名、剔除语义对不上的假阳性，如 "delete the agent" 撞上了不相关的 "delete this permission rule"），最终验证出 **19 条真实改写**，其中 13 条是词典里从未有过的全新条目（其余 6 条发现已被之前的维护补过）。全部 19 条在合并前逐条用 `buf.includes()` 做了字节级存在性校验，19/19 通过，无一条是臆造。

合并 → `build-dict` → 重新 `patch` → `scan` 复核：新增的 13 条全部命中（957 = 944 + 13），词典总量 1151→1165，覆盖率 70.4%→71.0%。剩下的未命中项**刻意保留在源词典里不删**——它们对更早版本的用户仍然有效，删掉只会丢失向后兼容性，删除的正确前提是"确认收录了 CC 早期版本已经很老、不会再有人用"而不是"这个版本用不上"。

**方法论小结**：滞后诊断不能只看"未命中"的字面文本重新翻译一遍——要先确认未命中的原因是"缺新词条"还是"旧词条描述的文案已不存在"，两者的修复动作完全不同。后者需要去当前二进制里找真正存在的现存文案，而不是重复劳动。

---

## 十一、2026-08-03 严重事故：补丁打出来的 claude.exe 启动即崩溃

### 现象

用户打完补丁后启动 `claude`，直接崩：

```
TypeError: ptr.cancel is not a function. (In 'ptr.cancel(error)', 'ptr.cancel' is undefined)
      at destroy (internal:streams/native-readable:100:15)
      at _destroy (internal:streams/destroy:63:18)
      at endReadableNT (internal:streams/readable:906:52)
Bun v1.4.0 (Windows x64 baseline)
```

### 排查中的一次错误判断（值得记下来）

第一反应是看 `~/.claude/daemon.log`，发现最后一条是
`upgrade self-respawn spawned but never became reachable within 45s`，
于是判定"是我连续 restore/patch 触发 daemon 反复自重启导致的竞态"，并让用户去开新终端验证。**这个因果是反的**：daemon 拿新二进制 respawn 起不来，正是"新二进制本身是坏的"的**结果**而非原因。日志时间线其实已经把答案写在脸上了：

- `02:20:05` 文件变化（restore → 纯英文）→ 重启**成功** `daemon start version=2.1.220`
- `02:22:09` 文件变化（patch → 汉化版）→ 重启**失败**（45s 内不可达）

纯英文能起、汉化版起不来——证据一直都在，只是被"我倾向于相信补丁没问题"的先入之见盖住了。

### 定位：隔离复现 + 自动二分

正确做法是**不碰用户环境**，在 scratchpad 里复制一份纯英文基线、打上补丁、用独立 `CLAUDE_CONFIG_DIR` 实际运行：

- `--version` → **正常**（这就是此前"验证通过"全是假阳性的原因：路径太浅）
- `doctor` → **崩溃**，与用户报错一字不差 → 确认是补丁的锅

然后写自动 delta debugging（`ddmin`）脚本二分词典。关键工程技巧：**先一次性预计算全部 4683 处替换位置**（复刻 patcher 的顺序与词边界守卫），之后每轮只需按子集把记录应用到干净副本 + 写盘 + 实跑，把每轮成本从 70s 降到几秒。17 次测试收敛到唯一元凶：

```
" to cancel"  ->  " 取消"   (命中 40 处)
```

### 根因：补丁扫的是整个二进制，不只是 Claude Code 的 JS

打印这 40 处命中的上下文，第 2 处性质完全不同：

```
@80565856
…toUTF16AllocSentinel·JSSentinel·…·oncancel·could not find stream to cancel·
preventCancel·boundResumableSinkCancel·boundAsyncIterableSourceCancel·flowlabel…
```

这是 **Bun 运行时的原生标识符表**，紧邻的全是流对象的属性名。把 `could not find stream to cancel` 里的 " to cancel" 覆写成中文+空格填充，破坏了这张表 → 原生流对象拿不到 `cancel` 方法 → `ptr.cancel is not a function`。其余 39 处（`Press Esc to cancel` 等）都是 Claude Code 自己的界面文案，无害。

精确验证（只排除这一处，其余 4682 处照打）：

```
[全量(对照)]     应用 4683 处 => 崩溃 ✗
[排除 @80565856] 应用 4682 处 => 正常 ✓
```

**这不是"一条词条没翻好"，是架构缺陷**：等长替换是对整个 253MB 做字节搜索替换，扫过的还有 Bun 运行时、SQLite / ripgrep / ICU / libjpeg 等第三方库的数据区。词边界守卫只挡标识符内部命中，挡不住这种"字符串字面量落在原生数据表里"。

### 修复

1. **运行时冒烟验证（最重要，`src/patcher.js: verifyRuntime`）**——补丁产物先落临时文件，用独立 `CLAUDE_CONFIG_DIR` 实跑 `--version` + `doctor`，**跑通才 rename 覆盖 target**；不通过就删临时文件并报错，用户的 claude.exe 一个字节都不会被改。这道网把整类"改坏二进制"问题从"用户下次启动才发现"变成"打补丁当场拦截"。已双向验证：好产物放行、故意注入那一处的坏产物被准确拦截并报出崩溃原因。
2. **危险词条黑名单（`tools/build-dict.cjs: DANGEROUS`）**——剔除 `" to cancel"`，代价是损失 39 处正常界面文案，覆盖率 71.0% → 70.9%。
3. `patch` 增加 `--no-verify` 逃生口（默认开启验证）。

### 教训

1. **「验证过才算完成」里的"验证"必须触达真实失败模式。** 字节断言 + esbuild 语法检查 + `--version` 全绿，但它们联合起来仍然漏掉了这个 bug——因为没有一项真正走完启动路径。绿灯的数量不等于覆盖度。
2. **自己写在文档里的建议也会是错的。** §七 那条"不要实跑 claude.exe，字节断言即可"是上一轮开发为了绕开超时问题留下的，结果直接埋了这颗雷。已改正。
3. **工具自己发出的告警不要跳过。** `scan` 两次都打印了"短词命中异常多，建议人工核查"，`" to cancel"` 就在名单里，两次都被无视了。
4. **因果方向要用证据钉死。** daemon 日志差点让人把结果当原因；先问"如果我的假设成立，日志还应该长什么样"，再回去核对。
5. **修 bug 前先把复现做成隔离的、可自动判定的。** 有了"复制副本 + 独立配置目录 + 实跑判崩"这个 harness，二分才敢全自动跑，也才敢在不打扰用户环境的前提下反复试。

---

## 十二、2026-08-03 推送前多智能体交叉审查：修复本身又被查出 5 个问题

修完 §十一 准备推送时，先跑了四路独立审查（架构评审 / patcher 逐行正确性 / 跨平台回归 / 验证机制与文档核对）。结果：**修复本身又被查出 5 个必须先处理的问题，其中 3 个是这次引入的。** 记录如下——这一节的价值在于说明「自己觉得没问题」有多不可靠。

### 查出的问题

1. **macOS/Linux 上 patch 会 100% 失败（最严重，且部分是存量）。**
   `fs.writeFileSync` 建出的新文件是 `0o666 & ~umask`（POSIX 上通常 0644，**无执行位**），而 `rename` **不继承目标路径原有的权限位**。于是 `verifyRuntime` 去 spawn 临时文件直接 EACCES → 验证必然失败 → patch 中止；更糟的是错误文案会把权限问题谎报成「词典破坏了二进制，请提 issue」。
   `restore()` 走同一条路 → 还原出的 `claude` 同样不可执行——**这条在本次改动前就存在，说明本工具在非 Windows 平台从未被端到端跑通过**，而 README 一直标着「macOS ✅ / Linux ✅」。
   修复：新增 `inheritExecMode()`，写完 tmp 立刻按 target 原 mode 补执行位；README 平台表改为「Windows 已实测 / macOS·Linux 未实测」——**在拿到真机验证前不写 ✅**。

2. **回滚失败被吞掉，错误信息与事实相反。**
   热替换慢路径里若「放回新文件」失败、回滚 `renameSync(aside, target)` **也**失败，异常被 `catch {}` 吞掉，用户看到的却是「已回滚，claude.exe 未被改动」——而此刻磁盘上根本没有 claude.exe。注释写着「保证 target 存在」，但代码只是**尝试**。这比不写这句话更糟：它会让唯一能自救的用户放弃自救。
   修复：区分回滚成功/失败两条消息；失败时把让位副本的绝对路径和手动恢复命令直接打出来。

3. **中断窗口无自愈。** 两次 rename 之间 target 短暂不存在，若此时被强杀/断电，下次运行会因 `isClaudeBinary` 先行拦截而报「不是有效的 Claude Code 二进制」，**完全不提旁边躺着完好的让位副本**，用户只能重装。
   修复：`healInterrupted()` 在 `isClaudeBinary` **之前**运行，自动从最新的让位副本恢复。

4. **热替换实际只能连用一次。** `aside` 名字写死为 `.cchans-old`，上次留下的若仍被老进程映射，`renameSync(target, aside)` 会撞 EPERM（实测确认）——而「不必关闭 Claude Code」正是它的卖点。
   修复：`aside` 带时间戳，`cleanAside()` 按前缀批量回收。

5. **`"Version: "` 正在破坏 WebSocket 握手（存量，且已在用户机器上生效）。**
   它命中 Bun 的握手请求头模板：`Sec-WebSocket-Version: 13` → `Sec-WebSocket-版本：13`，服务端按 RFC6455 必然拒绝 → Remote Control / IDE 集成 / ws 传输的 MCP 全部连不上。而 `--version`/`doctor` 都不开 WebSocket，**运行时验证全绿放行**。
   修复：加入 `DANGEROUS` 黑名单。

### 真正的收获：从「黑名单」升级为「结构守卫」

两路审查**各自独立**提出并验证了同一个判据，这个收敛信号很强：

事故的真机理不是「覆写了标识符表」这么笼统，而是**链接器的字符串尾部合并（tail-merge）**——实证：整个 78–84MB 原生区内**不存在独立的 `\0cancel\0` 表项**，JSC 属性名 `cancel` 就是消息 `could not find stream to cancel` 的**末 6 字节切片**。覆写消息尾部 = 直接摧毁属性名。

由此得到一个可泛化、纯字节级、与版本无关的判据：
> C/C++ 原生字面量池 = 「单个 NUL 分隔的紧凑短串」→ 邻域**孤立 NUL** 密集；
> 而 Bun 存放 CC 自己的 JS 字符串用 `{ptr,len}` 结构，周围是**成串**的 NUL 填充。

实测区分力（v2.1.220 全词典 4768 处命中）：精确标出事故元凶那一处（`" to cancel"` 40 处中的第 1 处 @80565856），界面文案**零假阳性**，全词典共标出 37~38 处 / 9 条词条（全是 Bun/SQLite/JSC 内部串）。已作为 `patchBuffer` 的第二道守卫（`inNativePool`）落地。

好处不只是防新雷：`" to cancel"` 因此**不必再整条拉黑**——守卫只跳过危险的那 1 处，另外 39 处正常界面文案照常翻译。**黑名单从「按词条一刀切」退回到只处理守卫看不见的那一类**（目前只剩 `"Version: "` 这种嵌在连续文本模板里的协议常量）。

### 教训

1. **修复也要被审查，而且要多路独立审查。** 四路里有一路（架构评审）给出「可以推送」的结论——因为它评审的是词典与架构轴，没检查执行位和回滚谎报。**结论要取并集，不能取任一路的结论当全部。**
2. **同一个陷阱会换层级重现。** §十一 的教训是「产物必须真跑通才算验证」，我把它写进了代码；但**验证本身只在一个平台上跑通过**，于是 macOS/Linux 的执行位问题原封不动地重演了一次。
3. **注释里的断言也是要验证的。** 「保证 target 存在」这句注释和代码实际行为不符，而它恰恰出现在最危险的分支上。
4. **工具自己的告警要接进流程，而不是打印出来靠人看。** `scan` 的「短词命中异常多」告警两次被无视，正是因为信噪比太低（名单里混了大量无害条目）。新的结构守卫把同一件事变成了**精确的、自动执行的跳过**，而不是一份需要人工判读的清单。

---

## 十三、2026-08-03 最深一层反转：字节码才是运行时真身，此前全部替换有效数为 0

### 现象与判定

用户重启后报告菜单乱码（`自动更新频道` 显示为 `èªå¨æ´æ°é¢é`）。用
`spawnSync(..., {encoding:'buffer'})` 抓进程**原始输出字节**判定：二进制里的字节是
正确 UTF-8，但运行时把它按**单字节(Latin-1)**逐字节解码——不是终端问题。
乱码检测器实测：16 条免登录命令的输出里，**正确渲染的中文为 0 个**。

### 根因：三层叠加

用零依赖解析 Bun 模块表（新增 `src/bunfmt.js`，纯靠尾部 trailer 反推：
`blobBase = trailer尾 - (byteCount + 48)`，不解析 PE/Mach-O/ELF）发现：

1. **主模块带 146.7MB JSC 预编译字节码**（cli.js 源码仅 20.7MB）。有字节码时
   Bun 不解析源码——对源码区的 2489 处替换**根本不生效**。
2. **字节码里的字符串常量按 8-bit(Latin-1) 存储**（16 字节记录头，`hashAndFlags`
   bit0 = is8Bit）——对字节码区的 2070 处替换**必然乱码**。
3. **模块 `encoding` 字节 = 1（非 UTF-8）**——即使源码被解析，中文也会被单字节解码。

即：此前引以为豪的「4596 处替换、验证通过」，**真实有效数为 0**——一半乱码、
一半无效。§三 的立论（"中文串活在运行中的 exe 里，Bun 照常执行"）验证的是
**能跑**，从没验证过**显示对不对**。

### 修法：三个等长字段改写 + 区域限定（仍零依赖）

- `bytecode.length` → 0（4 字节）：Bun 回退解析 JS 源码；
- `encoding` → 0 = UTF-8（1 字节）：源码按 UTF-8 解码；
- 替换严格限制在主模块源码区内（此区 100% ASCII，poolSkips 自然归零）。

实测：973 个中文正确渲染、零乱码。代价：**启动 189ms → ~1174ms（+1s）**——
无字节码需现场解析 20.7MB JS，用户知情后接受。覆盖 944 词条 / 2425 处，
**全部真实生效**。`verifyRuntime` 新增两类判定：`mojibake`（输出含 Latin-1 展开，
反解出 CJK 即报警）与 `noeffect`（能跑但零中文 = 补丁未生效）——好产物/乱码产物/
未打补丁三类实测全部正确分类。`scan` 同步改为只扫源码区。

### 尾声：最后的"乱码"是自己人

修复部署后用户仍见斜杠菜单乱码。逐一排除（进程启动时间、双路径同文件校验、
全部 2425 处替换点逐字节比对、磁盘缓存搜索、源码里 17 处 latin1 + 24 处 binary
逐一审读）后按两次失败法则复盘假设，发现从未验证的一条：**渲染菜单的进程跑的
是哪个镜像**。答案：正是本会话自己的 worker（14:46 启动，早于 15:08 修复；
daemon 日志 `adopted=8 respawned=0` 证明它跨过二进制更换被"接管"而非重启）——
用户的斜杠命令都在本会话执行，看到的菜单来自这个旧镜像进程。磁盘与用户自己的
交互进程均干净。处置：重启本后台会话即可。

### 教训

1. **「验证过才算完成」必须覆盖到"输出对不对"，不只是"跑没跑起来"。** 本项目
   两次大事故（§十一 崩溃、本节乱码）共用同一个病根：验证只测了浅路径。
2. **"部分正常"可能是"全部错误"的错觉**——doctor 里 22 个正常中文让我们以为
   补丁生效了 70%，实际那是唯一走了正确路径的零头。量化（正确中文计数）胜过抽样印象。
3. **排查显示类问题时，第一步先问"这个像素是哪个进程画的"**——今天在二进制里
   挖了几个小时，最后一处"乱码"根本不在二进制里。

---

## 十四、2026-08-04 全局长度预算引擎：突破等长上限，仍不解析可执行格式

用户提出：把约束从「每条译文必须等长」放宽为「整个 JS 源码区总长度不变」——
短译文省下的字节汇成全局预算，容纳超长译文，模块表一个字节不动，仍不需要
解析 PE/Mach-O/ELF。独立复算其测算数字（对 v2.1.220 源码区）：安全条目省
9683 字节，477 条超长词条里可命中的 387 条仅需 5138 字节，净余 4545——
本版可全量容纳，不必等重打包。

### 实现（`patchBuffer` 之外新增 `rewriteRegion`, `cchans patch --full`）

长词条优先认领不重叠区间（与等长路径同语义）；超长候选按成本从贵到贱排队，
预算够才接受，同一句话要么全译要么全不译；按位置重新拼接，若比原长短则在
**源码区末尾**补 ASCII 空格凑回原长（区末是语句边界，补空白语法无害）。

### 首跑即被自己建的验证网拦下两次，各修出一道结构性守卫

1. **`SyntaxError`**：超长条目命中了 `switch` 的 `default:` 等代码位置——
   等长路径只搜字节, 从不关心命中点是不是"字符串里"。建了**字符串分段器**
   `stringSegments`（手写 JS 词法扫描：单双引号、模板字面量、行/块注释、
   正则-除号歧义）。首版又在两处经典词法坑翻车（模板表达式 `${cond?{a}:b}`
   内的花括号没有按深度配对；`return /re/` 这类关键字后的正则被误判成除法），
   实测吞掉 41KB 源码，逐一修复后危险方向误判归零。
2. **审查发现「Usage credits」污染了 `e.includes("Usage credits are required for
   long context")` 这类比较谓词**——API 返回英文，谓词译成中文后条件永假，
   长上下文计费错误的识别路径静默失效。建了**比较谓词守卫**
   `isComparisonString`（`includes(`/`===`/`case` 上下文一律不译），
   同时接入默认等长路径，把存量的同类破坏（`t.includes("未找到")`）一并修掉。
   推送前审查又发现两条正则太窄召回不足（`case"` 无空格 6368 处 vs 带空格
   仅 26 处；`===` 贴身两侧；接收者位置 `"字面量".includes(x)`），当场收紧。

### 实测结果

`--full`：944→1160 词条命中、2425→4111 处替换（含超长 387 条/1467 处，
预算余量 5732 字节，零放弃）；doctor 中文输出 22→60 字符；双路径端到端实跑
验证通过；--full 幂等（两遍产物 sha 逐字节相同）；restore 逐字节还原原版；
真机 sha 与隔离测试产物一致；自测 34/34。

### 教训

1. **给用户的建议先独立复算，再动手实现。** 数字对上了才建引擎——省下的是
   "方案本身不成立"这类返工。
2. **新引擎第一次跑，安全网就该派上用场，而不是等它"顺利通过"。** 两次
   SyntaxError/谓词破坏都是首跑即被拦，说明验证链条本身没有虚设。
3. **"当前词典零暴露"不等于"没有召回缺口"。** 比较谓词守卫的正则收紧前后，
   真实二进制的替换数字完全不变——说明缺口是真实存在的，只是这一版词典没
   踩上；不能拿"复测数字没变"当作"问题不存在"的证据，要靠独立统计
   (`case"` 出现 6368 次)才能定论。
